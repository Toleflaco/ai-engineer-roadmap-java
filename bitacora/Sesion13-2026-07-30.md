# Sesión 13 — Tools de lectura de productos y proveedor en erp-mcp-server (parcial)

- **Fecha:** 2026-07-30
- **Fase:** 2 (Tool calling, agentes y MCP)
- **Duración:** ~5h en dos bloques: mañana (~5:45–9:00) y tarde (~13:30–15:15). Excede el techo de 4h/día. Se documenta como excepción consciente, no como norma.
- **Estado del roadmap:** Tres tools de lectura añadidas de las seis candidatas (`listLowStockProducts`, `findProductsByCategory`, `getSupplier`). Quedan tres pendientes.

## Contexto

Tras la Sesión 12 (starter Spring AI MCP Server y primera tool `getProduct` verificada end-to-end), esta sesión amplía la superficie de tools de lectura sobre el dominio Product, siguiendo el plan trazado al cierre de la sesión anterior: productos con stock bajo, listado de proveedores, pedidos por estado, facturas pendientes. De las seis tools candidatas se completan dos en este bloque de mañana.

## Objetivo de la sesión

Añadir `listLowStockProducts()` y `findProductsByCategory(String category)` a `ProductQueryTools`, fijando en el proceso una convención de naming para tools MCP (`get`/`list`/`find...By...`) y un criterio explícito para elegir entre derived query y `@Query` JPQL según si la comparación es campo-contra-parámetro o campo-contra-campo. Verificar ambas end-to-end con mcp-inspector antes de dar el bloque por cerrado.

## Trabajo técnico

### Tool 1: `listLowStockProducts()`

Ubicada en `ProductQueryTools`, dominio Product. Sin parámetros, retorna `List<ProductInfo>`.

Criterio de negocio: `stock <= minStock` (reorder point), no `stock < minStock`. Decidido explícitamente porque en ERPs reales `minStock` modela el punto de reorden; alcanzarlo ya es la señal para reponer. Consistente con las categorías del seed (ningún producto tiene `stock == minStock` exacto, así que el criterio A vs B no cambia el resultado observable, pero sí el significado semántico).

Query en `ProductRepository` mediante `@Query` JPQL, no derived query. Motivo: derived queries no expresan comparación entre dos columnas de la misma fila. JPQL sí:

```java
@Query("SELECT p FROM Product p WHERE p.stock <= p.minStock")
List<Product> findLowStockProducts();
```

Se extrajo un helper privado `toDto(Product)` en `ProductQueryTools` para centralizar el mapping `Product → ProductInfo` y eliminar duplicación. `getProduct` se refactorizó para consumirlo:

```java
productRepository.findById(id).map(this::toDto).orElse(null);
```

Decisión sobre `toDto`: método privado de instancia en el `@Service`, no static, no en el record `ProductInfo`, no clase `ProductMapper` aparte. Argumento: minimalismo consistente con el resto del proyecto y flexibilidad ante roce futuro (por si mañana depende de estado inyectado).

`@Transactional(readOnly = true)` obligatorio por acceso lazy a `product.getSupplier().getName()` en el mapping, con `open-in-view=false`.

Description del `@Tool` en inglés, plural, comunicando caso de uso:

> "List all products that have reached or fallen below their minimum stock threshold. Use this to identify which products need to be reordered. Returns product name, category, unit price, current stock, minimum stock threshold, and supplier name."

### Tool 2: `findProductsByCategory(String category)`

Ubicada en `ProductQueryTools`, dominio Product. Recibe `String category`, retorna `List<ProductInfo>`.

Derived query `findByCategory(String)` en `ProductRepository`, sin `@Query`. Contraste pedagógico deliberado con `findLowStockProducts`: campo contra parámetro → derived; campo contra campo → JPQL.

Decisión sobre tipo del parámetro: `String` en tool y repositorio (opción A), no enum. Coherente con el estado actual de `Product.category` como `String` en la entidad (la memoria heredada sugería enum, pero verificación empírica en el fichero mostró `String`). Si en Sesión 15 se sube a enum, refactor localizado.

Uso de `@ToolParam(description = ...)` sobre el parámetro `category` para enumerar valores válidos:

```java
@ToolParam(description = "Category name in uppercase (e.g., COFFEE, DAIRY, TEA, SWEETENERS, EQUIPMENT, PACKAGING).")
String category
```

Anotación de Spring AI aplicada al parámetro, no al método. Genera propiedad `description` en el JSON schema del parámetro, distinta del `description` de la tool. Guía al LLM sobre valores válidos sin adivinanza. Import: `org.springframework.ai.tool.annotation.ToolParam`.

Naming de la tool: `findProductsByCategory`, prefijo `find` para búsqueda con criterio. Convención establecida en esta sesión: `get*` para uno por identidad, `list*` para enumeración amplia, `find*By*` para búsqueda con criterio. Tres roles distintos para que el catálogo sea legible por el LLM sin leer descriptions.

### Tool 3: `getSupplier(Long id)`

Ubicada en `SupplierQueryTools` (clase nueva), dominio Supplier. Recibe `Long id`, retorna `SupplierInfo`.

Introduce por primera vez el patrón "un servicio de tools inyecta dos repositorios": `SupplierRepository` (nuevo, mínimo, solo hereda de `JpaRepository`) y `ProductRepository` (reutilizado del dominio Product).

Método derived query nuevo en `ProductRepository`:

```java
List<Product> findBySupplierId(Long supplierId);
```

Records nuevos:

- `SupplierInfo(Long id, String name, List<ProductSummary> products)` en el paquete `supplier`. Sin `contactEmail` intencionadamente: fuera del caso de uso actual del agente ReAct, evita exponer PII sin necesidad, se expondrá vía tool dedicada si aparece un caso de uso.
- `ProductSummary(Long id, String name)` en el paquete `product`. Ubicado por origen del dato (Product), no por consumidor (SupplierQueryTools), para evitar mudanza si mañana lo consume otra tool.

Relación `Supplier ↔ Product` mantenida unidireccional, respetando la decisión locked de Sesión 10 (bidireccional solo si el dominio lo pide). El agregado `Supplier` no incluye una colección de productos porque los productos son entidades independientes que referencian a un supplier, no una composición dentro del agregado supplier. Cuando `getSupplier` necesita la lista de productos suministrados, se obtiene mediante `ProductRepository.findBySupplierId`, no navegando desde `Supplier`.

El repositorio devuelve `List<Product>` (entidades), el servicio mapea a `List<ProductSummary>` con `.stream().map(...).toList()`, consistente con el patrón ya establecido en `ProductQueryTools`. Se descartó la alternativa de JPQL con constructor expression (`SELECT new dev.toleflaco...ProductSummary(...)`) por acoplar el FQN del record al string JPQL y romper la convención "el repo devuelve entidades".

Regla lockeada: cuando se aplana una relación en un DTO MCP, incluir `id` si el objeto expuesto puede ser sujeto de otra tool. `ProductSummary` incluye `id` para que un agente ReAct pueda encadenar `getSupplier` → identificar producto → `getProduct(id)`.

`SupplierQueryTools` añadido como segundo varargs en el existente bean `ToolCallbackProvider`, junto a `ProductQueryTools`. Se cumple la convención de Sesión 12 de un único `ToolCallbackProvider` para todo el servidor agregando servicios vía varargs.

Description del `@Tool` en inglés:

> "Get detailed information about a supplier by its ID. Returns supplier id, supplier name and all its products (id and name)."

## Verificación end-to-end con mcp-inspector

Con mcp-inspector v2, Streamable HTTP contra `http://localhost:8080/mcp`.

**`listLowStockProducts()`:**

- Devuelve 7 productos (verificado contra seed).
- SQL generado: 1 SELECT de `products` con `WHERE p.stock <= p.min_stock`, sin JOIN, más 6 SELECTs individuales a `suppliers`.
- Predicción antes de invocar: 1 + N suppliers únicos (no 1 + N productos), aplicando persistence context de primer nivel de Hibernate. Contraste explícito con la fórmula ingenua "N+1 = 1 + N productos". En este caso: 7 productos con 6 suppliers únicos (los ids 2 y 3 comparten "Andes Green Coffee Import") → 1 + 6 = 7 SELECTs. Predicción cumplida.
- Todos los `supplierName` poblados en la respuesta JSON.

**`findProductsByCategory("COFFEE")`:**

- Devuelve 6 productos (verificado contra seed).
- SQL generado: 1 SELECT de `products` con `WHERE p1_0.category = ?`, más 2 SELECTs a `suppliers`.
- Predicción antes de invocar: 6 productos, 2 suppliers únicos (Andes Green Coffee Import y Tostadores del Norte, 3 productos cada uno), esperados 1 + 2 = 3 SELECTs. Predicción cumplida al 100% (esta vez determinista desde el `data.sql`, no dependiente de casualidad).

**`getSupplier(1)`:**

- Devuelve `SupplierInfo` con id=1, name="Andes Green Coffee Import", y `products` con 3 entradas (ids 1, 2, 3: los tres cafés verdes).
- SQL generado: 2 SELECTs. Primero contra `suppliers` con `WHERE s1_0.id = ?`, segundo contra `products` con `WHERE p1_0.supplier_id = ?`.
- Predicción antes de invocar: 4 SELECTs (arrastre mental de "+1 supplier por producto" de tools anteriores). Predicción incorrecta, aparecieron 2. La diferencia reveló el punto pedagógico central de la tool: el mapping a `ProductSummary(id, name)` no toca la relación LAZY `Product → Supplier` (solo escalares), por lo que no hay lazy loads adicionales.
- `@Transactional(readOnly=true)` mantenido igualmente. Justificación documentada: consistencia con las demás query tools, seguridad ante refactors (si mañana `ProductSummary` incorpora `supplierName`, el lazy load aparece y sin transacción abierta lanzaría `LazyInitializationException`), y persistence context unificado entre las dos llamadas al repositorio.

## Errores útiles cometidos

- Description de `listLowStockProducts` inicialmente en singular ("Get detailed information about a product with stock..."), casi copia-pega de `getProduct`. Corregido tras señalar el problema: los descriptions son la única señal que tiene el LLM para distinguir tools; singular frente a plural es información semántica relevante para el modelo. Regla: describir qué hace y para qué se usa, verbo en plural si devuelve lista.
- Asunción implícita de que `Product.category` era enum, basada en memoria heredada del proyecto ("enums como STRING"). Verificación en el código real mostró que es `String` puro. Regla derivada: cuando la memoria heredada afirme algo sobre el código y el código esté delante, verificar antes de aceptar.
- Bug de compilación en `@ToolParam`: paréntesis de cierre perdido antes de `String category`. Patrón recurrente ya documentado: autocomplete o edición sin releer carácter a carácter. Detectado antes de guardar el fichero, no llegó a IDE.
- Primera versión del commit de `listLowStockProducts` describía infraestructura que ya existía desde Sesión 12 (`@Service` con `@Tool`, `ToolCallbackProvider`) y omitía lo realmente nuevo (`@Query` JPQL con columnas, helper `toDto` extraído, refactor de `getProduct`). Regla reforzada: el commit describe lo que cambia respecto al estado anterior, no las convenciones ya establecidas.
- Firma inicial `List<ProductSummary> findBySupplierId(Long)` en `ProductRepository`. Derived queries no mapean automáticamente entidades a records arbitrarios; Hibernate hubiera fallado en runtime al arrancar. Corregido antes de guardar el fichero: retorno cambiado a `List<Product>` y mapping en el servicio. Regla reforzada: repositorios devuelven entidades, servicios mapean a DTOs.
- Imports huérfanos (`Optional`, `Product`, `ProductInfo`) quedaron entre revisiones sucesivas del fichero `SupplierQueryTools`. Detectados en revisión, no en compilación. Regla derivada: pasar `Ctrl+Alt+O` como hábito antes de guardar cualquier fichero que haya sufrido más de una edición.

## Decisiones locked de la sesión

Se suman a las de Sesiones 10, 11, 11.5 y 12, sin sustituir ninguna:

- **Naming convention de tools MCP**: prefijo `get` para uno por identidad, `list` para enumeración amplia sin criterio, `find...By...` para búsqueda con criterio. Tres roles semánticos distintos, legibles por el LLM en el catálogo sin leer descriptions.
- **Helper de mapping en el `@Service`, no en el record ni en clase aparte**. Método privado de instancia `toDto(<Entity>)` en cada `<Domain>QueryTools`. Minimalismo consistente. Si mañana el mapping crece o se comparte, revisitable con `ProductMapper` explícito.
- **`@ToolParam(description = ...)` sobre el parámetro cuando el input admite un catálogo cerrado de valores o requiere formato específico** (case, unidades, formato de fecha). Enumeración de valores válidos en la description del ToolParam, no en la del `@Tool`, para separar responsabilidades en el JSON schema. Paquete `org.springframework.ai.tool.annotation.ToolParam`.
- **Reglas de description de `@Tool`**: (1) verbo en plural si la tool devuelve lista; (2) comunicar caso de uso además de mecánica; (3) siempre en inglés; (4) el LLM lee la description para decidir cuándo llamar la tool, no solo qué devuelve.
- **Criterio N+1 refinado**: con persistence context de primer nivel, la fórmula real es `1 + N suppliers únicos`, no `1 + N productos`. Diferencia observable en logs de Hibernate. Deuda técnica sobre N+1 se pospone hasta que el volumen o el análisis empírico lo justifique; no se introduce `JOIN FETCH` ni `@EntityGraph` en tools de lectura simples de esta sesión.
- **Regla `@Query` vs derived query**: campo contra parámetro → derived query; campo contra campo (misma fila) → `@Query` JPQL.
- **Deuda técnica menor sobre case sensitivity**: derived queries de Spring Data son case-sensitive por defecto. `findByCategory("coffee")` no encuentra `"COFFEE"`. Solución (`IgnoreCase` sufijo o normalización en el servicio) queda diferida hasta que aparezca el problema empírico.
- **Bidireccional vs unidireccional decidido por ciclo de vida del agregado, no por existencia**. Regla: si al borrar el "padre" tiene sentido borrar al "hijo" en cascada → composición → bidireccional puede justificarse (`PurchaseOrder ↔ PurchaseOrderLine`). Si al borrar el "padre" el "hijo" sigue viviendo autónomamente (`Supplier ↔ Product`) → referencia → unidireccional.
- **Regla de `id` en DTOs aplanados**: cuando un DTO MCP aplana una relación, la colección aplanada incluye `id` si el objeto puede ser sujeto de otra tool. Habilita cadenas de llamadas del agente ReAct sin adivinanza.
- **Repositorios devuelven entidades, servicios mapean a DTOs**. Regla explícita ahora que hay dos servicios de tools consistentes entre sí. Constructor expressions JPQL (`SELECT new dev.toleflaco...Summary(...)`) rechazadas por acoplar FQN al string.
- **`@Transactional(readOnly=true)` en tools de lectura por defecto**, aunque el mapping actual no toque relaciones LAZY. Justificación: consistencia, robustez ante evolución de DTOs, persistence context unificado.

## Commits generados

- `656cc9e` — `feat: expose listLowStockProducts as an MCP tool`
- `d079f59` — `feat: add findProductsByCategory MCP tool`
- `adbc7c8` — `feat: expose getSupplier as MCP tool with product listing`

Todos ya en `origin/main`.

## Roadmap de Fase 2 actualizado

- Sesión 10 ✓: repo + esqueleto + 3 entidades + 2 commits.
- Sesión 11 ✓ (parcial): `PurchaseOrderLine`, `Invoice`, `@OneToMany`, encapsulación de agregado, 4 commits.
- Sesión 11.5 ✓: seed data H2, `defer-datasource-initialization`, verificación en consola, 1 commit.
- Sesión 12 ✓: starter Spring AI MCP Server, primera tool `getProduct`, verificación end-to-end con mcp-inspector v2, 2 commits.
- Sesión 13 parcial: tres de las seis tools de lectura completadas (`listLowStockProducts`, `findProductsByCategory`, `getSupplier`).
- Sesión 14 (o Sesión 13.5): `getPurchaseOrder(Long id)`, con líneas aplanadas a DTO. Introduce el patrón `List<LineSummary>` con `productId + productName + quantity + unitPrice`.
- Sesión 15: CI/CD + Dockerfile + migración a PostgreSQL.
- Sesiones 16–20: agente ReAct `erp-purchasing-agent`, guardrails, human-in-the-loop, docker-compose, cierre Fase 2.

## Próximos pasos

- Próxima sesión: `getPurchaseOrder(Long id)` como bloque principal. Aplica la regla de `id` en DTOs aplanados a las líneas (`productId`, quizás `productName`, `quantity`, `unitPrice`). Discutir si `supplierName` va aplanado al top level del DTO (probable) o si se deja fuera para forzar al agente a hacer `getSupplier(supplierId)` (menos probable, más costoso para el agente).
- Días siguientes: `listPurchaseOrdersByStatus`, `getInvoicesByStatus`. Variaciones del patrón derived query + enum + DTO aplanado, densidad pedagógica menor.

## Deudas técnicas activas

Sin deudas nuevas específicas. Heredadas, sin cambios:

- `Product.unitPrice` sin `precision`/`scale` declarados. TODO inline para Sesión 15 al migrar a PostgreSQL.
- N+1 sobre `suppliers` en tools que devuelven listas de productos con proveedor aplanado. Pendiente de evaluar si merece `JOIN FETCH` o `@EntityGraph` cuando el volumen lo justifique.
- Case sensitivity en derived queries con inputs de texto libre. Pendiente decidir estrategia (`IgnoreCase` sufijo o normalización en el servicio).
- Refactor del `groupId` de `com.mtole` a `dev.toleflaco` en `task-manager-api` y `task-manager-microservices` (prioridad media).
- Ampliar `GlobalExceptionHandler` con `NoResourceFoundException` en `document-analyzer-ai` (prioridad baja).
- Migración de Claude Code del instalador npm al native installer (prioridad baja).
- `apt upgrade` pendiente en Ubuntu WSL (prioridad baja).
- Deudas pedagógicas de Fase 1 (divergencias documentadas en el README del hub).
- README de `erp-mcp-server` sin escribir. Decisión: se pospone hasta cierre de tools de lectura (Sesión 13.5 o inicio 14) o hasta antes del Dockerfile en Sesión 15.
